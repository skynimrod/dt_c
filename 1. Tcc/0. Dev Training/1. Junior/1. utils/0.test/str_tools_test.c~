#include <stdio.h>
#include "str_tools.h"
   
void main(void)
{
    char buf[]="    hello   world !   ";
   
    printf("\n%s\n", buf);
    printf("\n删除左面的空格:%s, buf=%s\n", ltrim(buf), buf );
    printf("\n删除中间的空格:%s\n", mtrim(buf));

    return;
}
